{
  "name": "@template/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/server.ts",
    "capture:title-fixture": "node --import tsx src/features/titles/fixture-capture-cli.ts",
    "lint": "vp lint src",
    "check": "npm run lint && tsc -p ../../tsconfig.json --noEmit",
    "test": "vitest run test",
    "build": "echo \"TODO: add api build\""
  },
  "dependencies": {
    "@proofkit/fmodata": "0.1.0-alpha.20",
    "@effect/sql-sqlite-node": "4.0.0-beta.66",
    "@template/config": "workspace:*",
    "@template/shared": "workspace:*"
  }
}

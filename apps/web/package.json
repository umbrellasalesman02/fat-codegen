{
  "name": "@template/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc -p ../../tsconfig.json --noEmit",
    "test": "echo \"TODO: add web tests\"",
    "build": "vite build"
  }
}

{
  "name": "@template/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "vp lint src test",
    "check": "npm run lint && tsc -p ../../tsconfig.json --noEmit",
    "test": "ROOT=$(git rev-parse --show-toplevel) && vitest run \"$ROOT/packages/config/test\" --config \"$ROOT/vite.config.ts\" --root \"$ROOT\"",
    "build": "echo \"TODO: add config build\""
  }
}

{
  "name": "@template/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "echo \"TODO: add config validation\"",
    "test": "echo \"TODO: add config tests\"",
    "build": "echo \"TODO: add config build\""
  }
}

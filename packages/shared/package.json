{
  "name": "@template/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "effect": "4.0.0-beta.66"
  },
  "scripts": {
    "lint": "vp lint src",
    "check": "npm run lint && tsc -p ../../tsconfig.json --noEmit",
    "test": "ROOT=$(git rev-parse --show-toplevel) && vitest run \"$ROOT/packages/shared/test\" --config \"$ROOT/vite.config.ts\" --root \"$ROOT\"",
    "build": "echo \"TODO: add shared build\""
  }
}

# Called from Title Objects

Set Variable [ $old_tt_id[1] ; GetScriptProperty ( "old_tt_id" ) ]
Set Variable [ $new_tt_id[1] ; GetScriptProperty ( "new_tt_id" ) ]
Set Error Capture [ ON ]

# Duplicate Package groups - preparing for packages to link via Old PrimaryKey from clone
Go to Layout [ Layout: "TABLE FAT Package Group" ; Animation: None ]
Perform Find [ ⚠️ RESTORE ⚠️ ]
If [ Get(foundCount)>0 ]
	Set Variable [ $foundCount[1] ; Get(foundCount) ]
	Set Variable [ $recordPosn[1] ; 1 ]
	Go to Record/Request/Page [ First ]
	Loop [ Flush: Always ]
		Set Variable [ $old_packagegroup_id[1] ; FAT Package Group::PrimaryKey ]
		Duplicate Record/Request
		Set Variable [ $recordPosn[1] ; $recordPosn  + 1 ]
		Set Field [ FAT Package Group::Title id ; $new_tt_id ]
		Set Field [ FAT Package Group::Old PrimaryKey from clone ; $old_packagegroup_id ]
		Exit Loop If [ $recordPosn > $foundCount ]
		Go to Record/Request/Page [ With dialog: OFF ; $recordPosn ]
	End Loop
End If



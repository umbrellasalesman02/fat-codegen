If [ FAT Title::PrimaryKey = $$lp_id ]
	# Delete only if you are 100% sure that this is the Title you recently created
	Delete Record/Request [ With dialog: OFF ]
Else
	Show Custom Dialog [ Title: "Error" ; Message: "Something went wrong when cancelling title creation." ; Default Button: "OK" ]
End If

Close Window
Select Window [ Name: $$currentWindow ]


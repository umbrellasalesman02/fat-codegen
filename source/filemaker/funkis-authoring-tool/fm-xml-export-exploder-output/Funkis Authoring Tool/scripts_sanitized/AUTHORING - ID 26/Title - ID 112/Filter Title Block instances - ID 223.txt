If [ Get(scriptParameter) = "Title" ]
	Set Field [ FAT Title::gPackage id ; "" ]
End If
Set Variable [ $block_id[1] ; FAT Title::gBlock ]
Set Variable [ $package_id[1] ; FAT Title::gPackage id ]
If [ $package_id = "" ]
	Perform Find [ ⚠️ RESTORE ⚠️ ]
Else
	Perform Find [ ⚠️ RESTORE ⚠️ ]
End If

